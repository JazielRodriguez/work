// import { useEffect, useState } from 'react'
// import axios from 'axios'
export const useFetch = (url: string, http: string) => {
  // const [data, setData] = useState<any>()
  // return { data }
}
